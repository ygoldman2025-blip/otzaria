commit fe776a9b9e72007cbcbb09eb97b69d91d022d1a5
Author: Y-PLONI <7353755@gmail.com>
Date:   Fri Dec 12 12:27:42 2025 +0200

    תיקון הבניה לאנדרואיד

diff --git a/.github/workflows/build-and-announce.yml b/.github/workflows/build-and-announce.yml
index 2ba6ae40..7c09a004 100644
--- a/.github/workflows/build-and-announce.yml
+++ b/.github/workflows/build-and-announce.yml
@@ -381,11 +381,23 @@ jobs:
       - name: Clone repository
         uses: actions/checkout@v4
 
+      - name: Free up disk space
+        run: |
+          echo "Disk space before cleanup:"
+          df -h
+          # מחיקת קבצים מיותרים כדי לפנות מקום
+          sudo rm -rf /usr/share/dotnet
+          sudo rm -rf /opt/ghc
+          sudo rm -rf /usr/local/share/boost
+          sudo rm -rf "$AGENT_TOOLSDIRECTORY"
+          echo "Disk space after cleanup:"
+          df -h
+
       - name: Set up Flutter
         uses: subosito/flutter-action@v2
         with:
           flutter-version: '3.35.7'
-          cache: true
+          cache: false  # השבתת cache כדי למנוע בעיות מקום
       
       - run: sudo apt install ninja-build
       
@@ -394,6 +406,17 @@ jobs:
           flutter pub get
           flutter build apk
       
+      - name: Clean build artifacts
+        if: always()
+        run: |
+          # ניקוי קבצי build מיותרים אחרי הבנייה
+          flutter clean || true
+          rm -rf build/app/intermediates || true
+          rm -rf build/app/.transforms || true
+          rm -rf ~/.gradle/caches || true
+          echo "Disk space after build cleanup:"
+          df -h
+      
       - name: Upload apk
         uses: actions/upload-artifact@v4
         with:
